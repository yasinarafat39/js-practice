

// the indexOf() methods returns the index(position) the first occurrence of a string in a string.
const text = "please locate where 'locate' occurs!";
const index = text.indexOf("locate");
console.log(index); // 7 
